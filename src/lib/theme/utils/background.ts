import { ThemeColorsValue } from '@lib/theme/utils/utils.types'

const Background = {
  // An abbreviated property for border-radius
  bg: (value: ThemeColorsValue) => ({
    backgroundColor: value,
  }),
}
export default Background
